# dependencies
node_modules

# build output
dist
dist-ssr

# vite / logs
*.log
.DS_Store

# env & secrets (never commit)
.env
.env*.local

# local app data — Anthropic key, tokens, database
settings.json
*.db
*.db-journal
*.sqlite

# Rust / Tauri build artifacts
src-tauri/target
src-tauri/gen
